// Reserved for future app-wide configuration.
//
// The previous hardcoded OWNER_ID / SESSION_SECRET configuration was removed when the app
// moved to Supabase Auth. Identity now comes from the Supabase session (see lib/auth-server).
export {};
