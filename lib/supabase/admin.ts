// Reserved. The app no longer requires a service-role key: registration and admin
// operator-creation are handled by SECURITY DEFINER RPCs (app_register_operator /
// app_admin_create_operator) callable with the public anon key. Kept as a placeholder in
// case a future feature needs privileged server access.
export {};
